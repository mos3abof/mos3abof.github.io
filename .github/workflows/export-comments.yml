name: Export comments (retired)

# The new-comment repository_dispatch event now triggers build.yml directly,
# which runs export_comments.py before zola build and deploys without committing
# any TOML files back to the repository.
#
# This file is kept to avoid 404s on any stored webhook references.
# It does nothing.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Comment export is now handled by build.yml on repository_dispatch."
